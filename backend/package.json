{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for landing page",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'No build step required for backend'",
    "dev": "nodemon index.js",
    "db:setup": "node database/setup.js",
    "db:reset": "node database/setup.js",
    "db:seed": "node -e \"require('./database/setup.js').setupDatabase()\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
